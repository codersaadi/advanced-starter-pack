export { default } from '@/components/auth/forgot-password';
import { APP_NAME } from '@/constants';
import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: `${APP_NAME} - Forgot Account Password`,
  description: '...',
};
